engine-strict=true
# Supply-chain guard: refuse versions younger than 7 days (in minutes)
# so freshly-published malicious releases have time to be flagged/yanked
# before pnpm install/update will pick them up. Existing lockfile pins
# are honoured regardless, so this is install/update-time only.
minimum-release-age=10080
