/**
 * Shared line colour for the facility cost/intensity charts.
 *
 * The price line and the emissions-intensity line both use this red so the
 * two measures read consistently across the facility dashboard. Keep them
 * pointed at this single token rather than duplicating the literal.
 */
export const LINE_COLOUR = '#e63946';
