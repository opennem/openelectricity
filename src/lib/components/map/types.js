/**
 * Shared types for the generic PointMap component.
 *
 * @typedef {{
 *   id: string | number,
 *   lng: number,
 *   lat: number,
 *   label: string,
 *   colour: string,
 *   radius: number,
 *   raw: Record<string, any>
 * }} MapPoint
 */

export {};
